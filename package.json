{
  "name": "ssi-matchfinder",
  "version": "1.0.0",
  "description": "Static web frontend for finding SSI (Shoot N Score It) local matches",
  "type": "module",
  "scripts": {
    "fetch": "node scripts/fetch-matches.js",
    "fetch:dump": "node scripts/fetch-matches.js --dump",
    "notify": "node scripts/discord-notify.js",
    "serve": "npx serve docs",
    "test": "NOMINATIM_DELAY_MS=0 node --test test/*.test.js"
  },
  "engines": {
    "node": ">=18"
  }
}
